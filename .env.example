# Configuration file for OpenEO's environment variables
# Copy this file as .env to the dedicated folder where your notebook resides and fill your values

# ==============================================================================
# Backend Configuration
# ==============================================================================
# Backend selection supported for pre-processing in this repository: CDSE and EOPF
BACKEND_PROVIDER = 

# OpenEO backend URL (must be HTTPS)
# Check OpenEO hub to see available services (https://hub.openeo.org/)
BACKEND_URL = https://sample-openeo-backend.com/

# ==============================================================================
# Spatial Extent Configuration (Dict Structure)
# ==============================================================================
# Bounding box coordinates (will be parsed as dict with west, south, east, north)
# Pass the floating values for the west, south, east, and north bounds of a selected location
SPATIAL_EXTENT_WEST = 
SPATIAL_EXTENT_SOUTH = 
SPATIAL_EXTENT_EAST = 
SPATIAL_EXTENT_NORTH = 

# ==============================================================================
# Data Collection Configuration
# ==============================================================================
# For example, Sentinel-2 collection ID as listed in the given openEO service
COLLECTION_ID =

# Band names as comma-separated list (will be parsed as list)
# Example for BAIS2: B04,B06,B07,B8A,B12
BAND_NAMES = 

# ==============================================================================
# Temporal Extent Configuration (List Structure)
# ==============================================================================
# Start and end dates of the satellite observation (will be parsed as list)
TEMPORAL_EXTENT_START = 2019-08-19
TEMPORAL_EXTENT_END = 2019-08-30

# # ==============================================================================
# # Processing Options
# # ==============================================================================
# # Output format: PNG, GTiff, NETCDF
# OUTPUT_FORMAT = PNG

# ==============================================================================
# Notes
# ==============================================================================
# - BACKEND_URL must be given in HTTPS
# - SPATIAL_EXTENT represents a bounding box in EPSG:4326 (WGS84) coordinate system
# - TEMPORAL_EXTENT uses ISO 8601 date format (YYYY-MM-DD)
# - BAND_NAMES should match the bands available in the specified collection
# - For Sentinel-2 L2A common bands in Copernicus Data Space Ecosystem OpenEO API:
#   * B02 (Blue), B03 (Green), B04 (Red) - RGB composite
#   * B05, B06, B07 (Red Edge) - Vegetation analysis
#   * B08 (NIR), B8A (Narrow NIR) - Vegetation indices
#   * B11, B12 (SWIR) - Fire detection, moisture content
