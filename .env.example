# Configuration file for OpenEO's environment variables
# Copy this file as .env to the dedicated directory where your notebook resides and fill your values

# ==============================================================================
# Backend Configuration
# ==============================================================================

# Backend providers supported for pre-processing in this repository: CDSE and EOPF 
# EOPF : https://api.explorer.eopf.copernicus.eu/openeo
# CDSE : https://openeo.dataspace.copernicus.eu/
BACKEND_PROVIDER = CDSE

# OpenEO backend URL (must be HTTPS) 
BACKEND_URL = https://openeo.dataspace.copernicus.eu

# ==============================================================================
# Spatial Extent Configuration (Dict Structure)
# ==============================================================================
# Bounding box coordinates (will be parsed as dict with west, south, east, north)
# Pass the floating values for the west, south, east, and north bounds of a selected location
# Example: Canary Island, Spain
SPATIAL_EXTENT_WEST = -15.91
SPATIAL_EXTENT_SOUTH = 27.73
SPATIAL_EXTENT_EAST = -15.29
SPATIAL_EXTENT_NORTH = 28.22

# ==============================================================================
# Data Collection Configuration
# ==============================================================================
# Specify the collection ID as listed in the selected openEO backend 
# Name may differ across backends
# Example: SENTINEL2_L2A in CDSE
COLLECTION_ID = SENTINEL2_L2A

# Band names as comma-separated list (will be parsed as list)
# Band names may differ across backends
# Example for BAIS2: B04, B06, B07, B8A, B12 in CDSE
BAND_NAMES = B04, B06, B07, B8A, B12

# ==============================================================================
# Temporal Extent Configuration (List Structure)
# ==============================================================================
# Start and end dates of the satellite observations in YYYY-MM-DD format (will be parsed as a list)
TEMPORAL_EXTENT_START = 2019-08-19
TEMPORAL_EXTENT_END = 2019-08-30

# ==============================================================================
# Notes
# ==============================================================================
# - BACKEND_URL must be given in HTTPS
# - SPATIAL_EXTENT represents a bounding box in EPSG:4326 (WGS84) coordinate system
# - TEMPORAL_EXTENT uses ISO 8601 date format (YYYY-MM-DD)
# - BAND_NAMES should match the bands available in the specified backend
# - For Sentinel-2 L2A common bands in CDSE OpenEO API:
#   * B02 (Blue), B03 (Green), B04 (Red) - RGB composite
#   * B05, B06, B07 (Red Edge) - Vegetation analysis
#   * B08 (NIR), B8A (Narrow NIR) - Vegetation indices
#   * B11, B12 (SWIR) - Fire detection, moisture content
